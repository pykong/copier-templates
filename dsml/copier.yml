_extra_paths:
- ~/Projects/.templates/.templates/

_tasks:
    - git init
    - pipenv install --pre --dev    
    - pipenv run nodeenv -pc
    - pipenv run npm init
    - subl --new-window --project ./[[ folder_name ]].sublime-project
    - fish -c "cd ."


